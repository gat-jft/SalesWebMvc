@model IEnumerable<SalesWebMvc_.Net7.Models.Seller>

@{
    ViewData["Title"] = "Sellers";
}

<h2>@ViewData["Title"]</h2>

<table class="table table-striped table-hover">
    <thead>
        <tr class="table-success">
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BirthDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BaseSalary)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BirthDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BaseSalary)
                </td>
                <td>
                    <a asp.asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    <a asp.asp-action="Details" asp-route-id="@item.Id">Details</a>
                    <a asp.asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>


@* COMENTÁRIO 1:
        "table-sucess" que eu coloco na class="", é para colocar uma cor
        de fundo VERDE (padrão do framework). *@

@* COMENTÁRIO 2:
        Na EXPRESSÃO LAMBDA do Html.DisplayFor(), é assim mesmo:
        A gente usa outro apelido (modelItem), e depois a gente
        chama este item (que é o item do foreach) *@